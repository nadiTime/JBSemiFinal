# JBSemiFinal
JBSemiFinal
for deployment:
  
  clone the repo
  
  npm update
  
  npm install
  
  Ruby Installer(for windows) 
  sudo su -c "gem install sass"(linux)
  
  gem install sass
  
run:
  
  sass scss/base.scss css/style.css
